# No Frontmatter Note

This note has **no YAML frontmatter** at all.

## Purpose

Test that the parser handles notes without `---` markers correctly.

The title should be derived from the filename: "no-frontmatter".

## Content

Just some regular markdown content with no metadata header.
