# M97 — Export to PDF/HTML

**Status:** ⏳ pending  
**Priority:** Low (85-99 range — address individually)

## Goal

Export the currently viewed note to PDF or HTML format.

## Notes

- Requires external library or tooling for PDF generation
- HTML export is simpler — just wrap rendered markdown in HTML template
- May require new dependencies (low-priority, needs justification)
- Terminal rendering and export rendering may differ in quality
